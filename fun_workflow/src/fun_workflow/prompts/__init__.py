"""Pure text: the questions the understanding models are asked and how their
answers read (`understanding`), the `/himonkey` persona (`chat`), and the
screenwriter (`drama`). No I/O; the LLM is injected by protocol.
"""
