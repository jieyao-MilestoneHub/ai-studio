"""Prompt builders: `h3.py` (MiniMax H3's structured video schema) and
`flux.py` (Flux's free-text image prompt), plus `convert.py`'s Chinese-to-
prompt conversion. Pure — no network calls, no provider imports.
"""
