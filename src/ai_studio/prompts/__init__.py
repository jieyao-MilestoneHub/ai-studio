"""Prompt builders: `h3.py` (MiniMax H3's structured video schema),
`flux.py` (Flux's free-text image prompt), `understanding.py` (the
understanding models' questions), and `convert.py`'s Chinese-to-prompt
conversion. Pure -- no network calls, no provider imports; the `LlmClient`
that rewrites is injected.
"""
