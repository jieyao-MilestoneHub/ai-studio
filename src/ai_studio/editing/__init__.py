"""The editing grammar: pacing, cuts, captions, audio, and delivery-format
policy (`format_policy.py`). No provider, render, or runtime imports, so the
rules stay testable with zero GPU. See `docs/editing-grammar.md`.
"""
