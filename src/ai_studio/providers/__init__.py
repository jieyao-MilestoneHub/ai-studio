"""The clip/image provider protocol and registry (`providers/registry.py`).
`ComfyUIProvider` (H3) and `FluxComfyUIProvider` drive the shared GPU pod;
`StubProvider` is the offline backend CI runs against.
"""
