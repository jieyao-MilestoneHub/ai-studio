"""The request queue (`pipeline/queue.py`), the LLM/Flux prompt-conversion
worker (`pipeline/convert_worker.py`), and the window-drain loop
(`pipeline/drain.py`) that ties the LINE bot to whichever GPU pod is open.
"""
